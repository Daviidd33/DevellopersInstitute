// Exercise 1
const tasks = [];

// 1.Create a function called addTask(). 
// As soon as the user clicks on the button:
// check that the input is not empty,
// then add it to the array (ie. add the text of the task)
// then add it to the DOM, below the form 
// (in the <div class="listTasks"></div>) .
// Each new task added should have 
// (starting from left to right - 
// check out the image at the top of the page)
// a “X” button. Use font awesome for the “X” button.
// an input type="checkbox". The label of the input is the task added by the user.
const form = document.getElementById("form")

function addTask() {

}